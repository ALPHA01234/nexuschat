# NexusChat client

This folder is the API-backed browser interface served by `server/server.js`.

Do not open `index.html` directly from disk for normal use. Start/deploy the backend and use the backend URL so authentication, Socket.IO, uploads, policies, and admin APIs work correctly.
